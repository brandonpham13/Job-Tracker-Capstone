{
    "version": 2,
    "outputDirectory": "client",
    "buildCommand": "cd server && npm install",
    "rewrites": [
        {
            "source": "/api/:path*",
            "destination": "/server/src/api.ts"
        }
    ]
}